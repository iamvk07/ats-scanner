{
  "name": "ats-scanner-monorepo",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r run typecheck",
    "dev:web": "pnpm --filter web dev",
    "dev:cli": "pnpm --filter cli dev",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
